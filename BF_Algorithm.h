#pragma once
#include <chrono>
#include <string>
#include "List.h"
#include "Matrix.h"
using namespace std;

double algorythm(shared_ptr<list>, int);
double algorythm(shared_ptr<g_matrix>, int);
void results(string[], int[], int, int);